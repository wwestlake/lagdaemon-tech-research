# 02 - Functional Language Paradigm

## Focus
Exploring immutable values, pure functions, higher-order functions, lambda closures, tail-call optimization (TCO), and expression-oriented AST evaluation.

## Key Concepts
* Expression evaluation (everything yields a value).
* Lambda functions & environment capture (closures).
* Tail-call elimination in LLVM IR.
* Pattern matching AST representations.
