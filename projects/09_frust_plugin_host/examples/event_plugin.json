{
    "name": "event_plugin",
    "version": "0.1.0",
    "description": "Example plugin exercising event subscription - registers a handler in on_init and expects the host to have a counter callback available",
    "sourceFile": "event_plugin.frust",
    "entryPoints": ["on_init"],
    "listenedEvents": ["something_happened"],
    "requiredHostFunctions": [
        { "name": "test_mark_fired", "description": "Called by my_handler when 'something_happened' fires - increments a host-side counter the test observes." }
    ],
    "requiredHostApiVersion": "0.1.0"
}
